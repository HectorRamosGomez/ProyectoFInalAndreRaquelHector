<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="OnlySave"
        android:roundIcon="@drawable/logo_bueno"
        android:supportsRtl="true"
        android:theme="@style/Theme.ProyectoFinalDAM">
        <activity
            android:name="com.example.proyectofinaldam.login.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".login.MainActivity2Idiomas" android:exported="false" />
        <activity android:name=".login.pagina_principal.MainAplicacion" android:exported="false" />
        <activity
            android:name=".login.pagina_principal.PaginaGastosActivity"
            android:exported="false" />
    </application>

</manifest>